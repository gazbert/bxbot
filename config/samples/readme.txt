Sample BX-bot Configurations  示例 BX-bot 配置
============================

Each folder contains sample config for running against a particular Exchange.每个文件夹都包含针对特定 Exchange 运行的示例配置。
 
Copy the sample YAML files for the chosen exchange into the ../config folder.将所选交换的示例 YAML 文件复制到 ../config 文件夹中。
